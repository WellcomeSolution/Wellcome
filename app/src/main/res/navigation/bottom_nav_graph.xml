<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/nav_graph"
        app:startDestination="@id/fragment_services">

    <fragment
            android:id="@+id/fragment_services"
            android:name="com.example.wellcome.ServicesFragment"
            android:label="@string/services"
            tools:layout="@layout/fragment_services" >
    </fragment>

    <fragment
            android:id="@+id/fragment_favorites"
            android:name="com.example.wellcome.FavoritesHostFragment"
            android:label="@string/favorites"
            tools:layout="@layout/fragment_favorites_host" >
    </fragment>

    <fragment
            android:id="@+id/fragment_profile"
            android:name="com.example.wellcome.ProfileFragment"
            android:label="@string/profile"
            tools:layout="@layout/fragment_profile" >
    </fragment>

    <fragment
            android:id="@+id/fragment_login_menu"
            android:name="com.example.wellcome.LogInMenuFragment"
            android:label="@string/LoginMenu"
            tools:layout="@layout/fragment_log_in_menu" >
    </fragment>
</navigation>